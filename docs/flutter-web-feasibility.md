# Feasibility: Flutter web PWA for the Gogios status page

Investigation for task `sz0`. Date: 2026-08-13.

**Question:** can the HTML output of Gogios become a progressive Flutter web app that
runs entirely in the browser and fetches a static JSON blob, so alerts can be
filtered, expanded, searched?

**Constraint (from the user):** the OpenBSD frontends must serve *static assets only*.
No application server, no CGI, no reverse proxy to a backend.

**Verdict: technically feasible, but not recommended.** The interactive-page goal is
sound and the data half is already done. Flutter is the wrong vehicle for it — it adds a
multi-GB toolchain and a multi-megabyte client payload to a zero-dependency Go project
whose entire dataset is 6.9 KB gzipped. A vanilla-JS single-page app reaches the same
goal at roughly 1/100th the payload and no new build dependency.

## 1. Current state (measured, not assumed)

The static-JSON half of the request **already exists** and is live in production.

- `internal/json_report.go` writes a JSON blob next to the HTML file
  (`jsonReportPath()` swaps the extension), atomically via temp-file + rename,
  driven from `internal/run.go:96`.
- `internal/html.go` writes a server-rendered HTML page with inline CSS and a
  `<meta http-equiv="refresh" content="300">` full-page reload.
- Both are served from `/var/www/htdocs/buetow.org/self/gogios` by OpenBSD `httpd`
  (`conf/frontends/etc/httpd.conf.tpl:197`), already static-assets-only.

Measured against the live endpoint on 2026-08-13:

| Item | Value |
|---|---|
| `https://gogios.buetow.org/` | 200, `text/html`, **91,807 B** |
| `https://gogios.buetow.org/index.json` | 200, `application/octet-stream`, **110,534 B** |
| `index.json` gzipped (level 9) | **6,895 B** |
| Checks in the blob | 459 OK, 37 suppressed, 1 warning, 0 critical/stale |

So the client already downloads ~92 KB of HTML every 5 minutes to render what is
6.9 KB of gzipped data. That is the actual problem worth fixing, and it is
independent of which frontend technology is chosen.

Two immediate findings that apply regardless of the decision:

1. **`index.json` is served as `application/octet-stream`.** OpenBSD `httpd`'s built-in
   media types cover only `text/css`, `text/html`, `text/plain`, `image/gif`,
   `image/png`, `image/jpeg`, `image/svg+xml` and `application/javascript`; everything
   else falls back to `application/octet-stream`. `fetch()` + `Response.json()` ignore
   the Content-Type, so this works today, but it needs a `types` block in
   `httpd.conf.tpl` to be correct.
2. **Nothing is compressed.** `httpd` has no on-the-fly gzip; it has `gzip-static`,
   which serves `<file>.gz` when the client accepts gzip. Writing `index.json.gz`
   alongside `index.json` would cut the transfer from 110 KB to ~7 KB. This is the
   single highest-value change in this whole investigation and costs about 20 lines
   of Go.

## 2. Does Flutter web satisfy the static-assets-only constraint?

Yes. `flutter build web` emits a purely static `build/web` directory, and PWA support
is generated by default: `manifest.json` and `flutter_service_worker.js` are produced
without extra configuration, and the generated `index.html` registers the service
worker. So "progressive web app served as static files" is genuinely out-of-the-box.

The constraint is satisfiable. The objections below are about cost, not possibility.

## 3. Costs and blockers specific to this deployment

### 3.1 Build toolchain (the biggest objection)

`go.mod` currently declares exactly one requirement, `github.com/magefile/mage`, and it
is marked indirect — Gogios is a pure-stdlib Go program. It ships as a signed OpenBSD
package built via `mage buildOpenbsd` and installed with `pkg_add` from
`pkgrepo.f3s.buetow.org`, plus a FreeBSD variant.

Adding Flutter means adding the Flutter/Dart SDK (multi-GB, no OpenBSD port) to the
build path of that package. In practice the web bundle would have to be built on Linux
and either committed as generated artifacts or embedded via `go:embed` (the project
uses no `go:embed` today). Either way, a self-described "lightweight and minimalistic"
monitoring tool acquires a heavyweight cross-language build dependency.

### 3.2 Payload

Flutter web's baseline is ~1.5–2 MB gzipped for the renderer alone (CanvasKit is
~1.5 MB on its own) before any application code. Against 6.9 KB of gzipped data that is
a 200×–300× overhead for the shell. A service worker caches it after the first load, so
steady-state cost is bounded — but the first load on a phone, which is exactly when you
are checking whether the servers are on fire, is the worst case.

### 3.3 Serving requirements

- **MIME types.** `.wasm`, `.json` and `.mjs` are not in `httpd`'s built-in list, so a
  `types` block must be added to `conf/frontends/etc/httpd.conf.tpl`. Serving `.wasm` as
  `application/octet-stream` breaks `WebAssembly.instantiateStreaming`.
- **COOP/COEP.** Flutter's Wasm build needs `Cross-Origin-Opener-Policy: same-origin`
  and `Cross-Origin-Embedder-Policy: credentialless` (or `require-corp`) for
  multi-threaded rendering. OpenBSD `httpd` *does* support this via `header set`, so
  this is solvable — it was my initial assumption that it was not.
- **iOS is a hard fallback.** WasmGC is unavailable in WebKit, and all iOS browsers are
  required to use WebKit, so on iPhone the app always falls back to the CanvasKit JS
  renderer. Firefox and Safari WasmGC support is still tracked by open bugs.

### 3.4 Rendering model

Flutter web paints into a canvas. For a page that is ~500 lines of status text, that
means no browser find-in-page, no native text selection, no useful view-source, and
accessibility only through Flutter's semantics bridge. For a text-dense operational
dashboard, canvas rendering removes capabilities the current plain-HTML page has for
free.

## 4. Recommended alternative

A vanilla-JS single-page app delivers everything the task actually asks for — filter,
expand details, search across all alerts, installable PWA, entirely browser-side against
a static JSON blob — with none of the above costs:

- One HTML file with inline CSS/JS, embedded in the binary via `go:embed` and written
  out by `persistHTMLReport` instead of the current 92 KB of generated markup.
- Estimated 10–15 KB uncompressed, ~4 KB gzipped, versus 1.5–2 MB.
- Add `manifest.json` + a small service worker for the PWA half; both are static files.
- No new build dependency, no change to the OpenBSD packaging, and the frontends keep
  serving static assets only.
- The page keeps working as plain text if JS is disabled if the fallback is kept.

The one genuine argument for Flutter would be sharing code with an existing Flutter
mobile client. No such client exists in this project.

## 5. Suggested follow-up tasks

In priority order, all independent of the Flutter decision:

1. **Write `index.json.gz` alongside `index.json`** and enable `gzip-static` in the
   gogios `httpd` server block. 110 KB → ~7 KB per poll. Highest value, lowest cost.
2. **Add a `types` block** to `conf/frontends/etc/httpd.conf.tpl` so `.json` is served
   as `application/json`.
3. **Replace the generated HTML with an embedded JS SPA** that fetches `index.json`,
   with client-side filtering, expandable details, and search over all sections.
4. **Add `manifest.json` + service worker** to make it installable and offline-capable.

## 6. Sources

- [OpenBSD httpd.conf(5)](https://man.openbsd.org/httpd.conf.5) — built-in media types,
  `header set`, `gzip-static`.
- [Flutter: Support for WebAssembly (Wasm)](https://docs.flutter.dev/platform-integration/web/wasm)
  — COOP/COEP headers, WasmGC browser support, iOS/WebKit limitation.
- [Flutter: Build and release a web app](https://docs.flutter.dev/deployment/web) —
  static `build/web` output, release-mode minification and tree shaking.
- [Flutter: Building a web application](https://docs.flutter.dev/platform-integration/web/building)
  — build output layout.
- [flutter/flutter: service worker generation](https://github.com/flutter/flutter/commit/9f145f6ced26dfcafdd5ec4b38f3d31e10ff9971)
  — `flutter_service_worker.js` generated by the build.
- [Flutter Web & WebAssembly in 2026](https://amgres.com/blog/flutter-web-webassembly-wasm-2026-guide)
  and [Flutter Web & Desktop 2026](https://softaims.com/blog/flutter-web-desktop-production-ready-2026)
  — CanvasKit/Skwasm bundle-size figures (third-party, treat as indicative).
